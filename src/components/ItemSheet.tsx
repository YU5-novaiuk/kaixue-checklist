'use client'
import { useEffect,useState } from 'react'
import { Lightbulb,Trash2 } from 'lucide-react'
import { Modal } from './ui/Modal'
import { ConfirmDialog } from './ui/ConfirmDialog'
import { useChecklist } from '@/hooks/useChecklist'
import { ChecklistItem,ItemStatus,ItemType,Priority } from '@/types/checklist'
import { itemTypeLabels,priorityLabels,statusLabels,statusesByType } from '@/lib/constants'
import { uid } from '@/lib/utils'
import { useToast } from './ui/Toast'
const defaultStatus=(type:ItemType):ItemStatus=>type==='physical'?'unchecked':type==='document'?'unprepared':'todo'
const blank=(categoryId:string):ChecklistItem=>({id:uid(),name:'',categoryId,itemType:'physical',priority:'recommended',status:'unchecked',quantity:1,tags:[],isSystemItem:false,createdAt:new Date().toISOString(),updatedAt:new Date().toISOString()})
export function ItemSheet({open,onClose,item,initialCategory='documents'}:{open:boolean;onClose:()=>void;item?:ChecklistItem;initialCategory?:string}){const {data,addItem,updateItem,removeItem}=useChecklist();const [draft,setDraft]=useState<ChecklistItem>(item||blank(initialCategory));const [confirm,setConfirm]=useState(false);const toast=useToast();useEffect(()=>{setDraft(item?{...item}:blank(initialCategory))},[item,open,initialCategory]);const set=<K extends keyof ChecklistItem>(key:K,value:ChecklistItem[K])=>setDraft(d=>({...d,[key]:value}));const save=()=>{if(!draft.name.trim())return;const next={...draft,name:draft.name.trim(),updatedAt:new Date().toISOString()};if(item)updateItem(next);else addItem(next);toast(item?'保存成功':'已添加到清单');onClose()};const changeType=(type:ItemType)=>setDraft(d=>({...d,itemType:type,status:defaultStatus(type)}));return <><Modal open={open} onClose={onClose} title={item?'编辑事项':'添加事项'} wide><div className="form-grid"><label className="field full"><span>事项名称 *</span><input value={draft.name} autoFocus placeholder="要准备什么？" onChange={e=>set('name',e.target.value)}/></label>{draft.helperText&&<div className="system-tip full"><Lightbulb/><div><strong>系统建议</strong><p>{draft.helperText}</p></div></div>}<label className="field"><span>分类 *</span><select value={draft.categoryId} onChange={e=>set('categoryId',e.target.value)}>{data.categories.filter(x=>!x.hidden).map(c=><option value={c.id} key={c.id}>{c.name}</option>)}</select></label><label className="field"><span>事项类型</span><select value={draft.itemType} disabled={draft.isSystemItem} onChange={e=>changeType(e.target.value as ItemType)}>{Object.entries(itemTypeLabels).map(([v,n])=><option key={v} value={v}>{n}</option>)}</select></label><label className="field"><span>状态</span><select value={draft.status} onChange={e=>set('status',e.target.value as ItemStatus)}>{statusesByType[draft.itemType].map(v=><option value={v} key={v}>{statusLabels[v]}</option>)}</select></label><label className="field"><span>必要程度</span><select value={draft.priority} onChange={e=>set('priority',e.target.value as Priority)}>{Object.entries(priorityLabels).map(([v,n])=><option value={v} key={v}>{n}</option>)}</select></label><label className="field"><span>数量</span><input type="number" min="1" value={draft.quantity||''} onChange={e=>set('quantity',Number(e.target.value)||undefined)}/></label><label className="field"><span>预计价格</span><input type="number" min="0" value={draft.estimatedPrice??''} placeholder="¥" onChange={e=>set('estimatedPrice',e.target.value?Number(e.target.value):undefined)}/></label><label className="field"><span>实际价格</span><input type="number" min="0" value={draft.actualPrice??''} placeholder="¥" onChange={e=>set('actualPrice',e.target.value?Number(e.target.value):undefined)}/></label><label className="field"><span>购买平台</span><input value={draft.purchasePlatform||''} placeholder="例如：淘宝" onChange={e=>set('purchasePlatform',e.target.value)}/></label><label className="field"><span>放置位置</span><select value={draft.luggageId||''} onChange={e=>set('luggageId',e.target.value||undefined)}><option value="">暂未分配</option>{data.luggage.map(l=><option value={l.id} key={l.id}>{l.name}</option>)}</select></label><label className="field"><span>提醒日期</span><input type="date" value={draft.reminderDate||''} onChange={e=>set('reminderDate',e.target.value)}/></label><label className="field"><span>标签</span><input value={draft.tags.join('、')} placeholder="宿舍、到校前" onChange={e=>set('tags',e.target.value.split(/[、,，]/).map(x=>x.trim()).filter(Boolean))}/></label><label className="field full"><span>备注</span><textarea rows={3} value={draft.note||''} placeholder="填写你的个人备注" onChange={e=>set('note',e.target.value)}/></label></div><div className="sheet-footer">{item?<button className="text-danger" onClick={()=>setConfirm(true)}><Trash2/>删除事项</button>:<span/>}<button className="primary" disabled={!draft.name.trim()} onClick={save}>保存</button></div></Modal><ConfirmDialog open={confirm} title="删除这个事项？" description="删除后无法恢复。" onCancel={()=>setConfirm(false)} onConfirm={()=>{removeItem(draft.id);toast('事项已删除');setConfirm(false);onClose()}}/></>}
