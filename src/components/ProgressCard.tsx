import Link from 'next/link'
export function ProgressCard({done,total,percent,href}:{done:number;total:number;percent:number;href?:string}){const card=<section className="progress-card"><div className="progress-row"><div><strong>{done}</strong><span> / {total} 项必需事项已准备</span></div><b>{percent}%</b></div><div className="progress-track"><i style={{width:`${percent}%`}}/></div></section>;return href?<Link href={href} className="progress-link" aria-label="查看已准备事项">{card}</Link>:card}
