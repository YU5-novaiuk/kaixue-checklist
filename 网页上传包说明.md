# 开学准备助手 v1.3.0 GitHub Pages 更新包

这个压缩包用于覆盖更新 GitHub 仓库 `YU5-novaiuk/kaixue-checklist`。

## 上传时注意

- 解压后，上传最外层文件夹里面的全部内容，不要只上传 `src` 或 `out`。
- `.github` 文件夹必须一并上传，它负责自动构建和发布 GitHub Pages。
- 不需要上传 `node_modules`、`.next`、`.next-dev`、`out` 或 `.git`。
- 网站数据仍保存在每位访问者自己的浏览器中，不同用户不会共享清单。

## 本版本

- 版本：v1.3.0-local
- 数据版本：5
- GitHub Pages 工作流：`.github/workflows/deploy-pages.yml`
